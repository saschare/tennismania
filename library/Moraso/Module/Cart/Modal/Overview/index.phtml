<div class="row">
    <div class="large-12 columns">
        <h1>Warenkorb</h1>

        <?php if (count((array) $this->articles) > 0) : ?>
            <table id="checkout_table_overview">
                <thead>
                    <tr>
                        <th width="100"></th>
                        <th>Beschreibung</th>
                        <th class="text-right" width="50">Menge</th>
                        <th class="text-right" width="100">Einzelpreis</th>
                        <th class="text-right" width="100">Gesamtpreis</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($this->articles as $article) : ?>
                        <tr>
                            <td>
                                <img src="<?php echo $article->mainimage ?>" alt="<?php echo $article->pagetitle ?>" title="<?php echo$article->pagetitle ?>" />
                            </td>
                            <td>
                                <h2><?php echo $article->pagetitle ?></h2>

                                <p><?php echo $article->summary ?></p>
                            </td>
                            <td class="text-right"><?php echo $article->qty ?></td>
                            <td class="text-right"><?php echo $article->price ?></td>
                            <td class="text-right"><?php echo $article->price_total ?></td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        <?php else: ?>
            <p>Leider haben Sie noch keine Produkte in Ihren Warenkorb gelegt.</p>
        <?php endif; ?>
    </div>
</div>

<div class="row">
    <div class="large-12 columns">
        <?php if (count((array) $this->articles) > 0) : ?>
        <a href="#checkout" class="right small openCheckout button">zur Kasse &raquo;</a>
        <?php endif; ?>
    </div>
</div>

<a class="close-reveal-modal">&#215;</a>